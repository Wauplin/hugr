# An agent binary from Python

In this tutorial you'll take an agent crate you already built, using `examples/hugr-docs`, and ship it to Python users in three ways: as a **typed wheel** generated by `hugr build --surface python`, as a plain **subprocess** speaking the JSON ask/answer contract, and as an **MCP server** via `--mcp-serve`. All three wrap the exact same bundle; you'll learn what each surface gives you and when to reach for which. For errors as answers, mandatory `AnswerMeta`, and trace lineage, see [the Ask and Answer contract](../agents.md#the-ask-and-answer-contract).

## What "shipping to Python" means here

A Hugr agent is defined once, in its crate folder (`hugr.toml` + `SYSTEM.md` + an optional Rust response contract). The Python wheel is *generated from* that definition; you never write Python to ship it. This is deliberately distinct from tutorial 05, where you *define* an agent in Python: here Python is only a consumer of an already-built agent.

## Prerequisites

- A working `hugr` CLI (`cargo install --path crates/hugr-toolkit` from the repo root, or your existing install).
- [maturin](https://www.maturin.rs) on your PATH (`pip install maturin`) as the wheel builder.
- An API key for the agent's provider. `hugr-docs` reads `HUGR_DOCS_API_KEY` (see its `hugr.toml`).

## Surface 1: the typed wheel

From the repo root:

```bash
hugr build examples/hugr-docs --surface python --release
```

This first builds the ordinary CLI binary. The `python` surface runs that binary with `--config --json` to read the compiled response schema, so the Python types come from the same schemars output seen by the model. It then creates a maturin "mixed" project under `examples/hugr-docs/dist/hugr_docs-python/` and builds a wheel:

```text
hugr_docs-python/
  Cargo.toml           # cdylib `_native`, links hugr-toolkit + the agent crate
  pyproject.toml       # maturin backend, module-name = hugr_docs._native
  bundle.bin           # the embedded agent bundle (same bytes as the CLI shim)
  src/lib.rs           # PyO3: ask_json(...) -> Answer JSON, in-process
  python/hugr_docs/
    __init__.py        # typed ask(...) -> Answer
    _models.py         # generated dataclasses: DocsResponse, Answer, AnswerMeta, BlobHandle
    py.typed           # PEP 561 marker
```

The build prints the wheel path; install it:

```bash
pip install examples/hugr-docs/dist/hugr_docs-python/target/wheels/hugr_docs-*.whl
```

The module name is the agent name with dashes mapped to underscores (`hugr-docs` → `hugr_docs`). Use it:

```python
import hugr_docs

answer = hugr_docs.ask("./docs", "How do I resume a trace?")
if answer.ok:
    print(answer.response)          # a typed DocsResponse dataclass, not a dict
else:
    print("error:", answer.error)
print(answer.trace_id, answer.metadata.cost_micro_usd)
```

Three things to notice:

- **Declared runtime args become typed parameters.** `hugr-docs` declares a positional, required `docs_path` runtime arg in its manifest, so the generated `ask()` signature is `ask(docs_path, question, *, trace_id=None, blobs=None, extra=None)`; positional args lead, before the question, mirroring the CLI surface exactly.
- **The typing is strict but there is no second validator.** Rust already casts the model's final JSON into the agent's response type before it reaches `Answer.response`; `_models.py` only deserializes that already-valid JSON into dataclasses.
- **It runs in-process.** The PyO3 bridge embeds the bundle and drives the real runtime inside your Python process; no subprocess, no serialization boundary beyond one JSON string. Traces still land under `~/.hugr/hugr-docs/` like every other surface.

Resume works the same as everywhere else; pass a previous answer's `trace_id` and you get a *new* trace with `depends_on` set:

```python
follow_up = hugr_docs.ask("./docs", "And how do I fork one?", trace_id=answer.trace_id)
```

## Surface 2: the binary as a subprocess

If you do not want to build or install a wheel, for example from a language without bindings or when keeping the agent fully out of process, every built binary already speaks the JSON contract: one `Answer` object on stdout, diagnostics on stderr, exit 0 even for error answers (errors are answers).

```bash
hugr build examples/hugr-docs --release   # the default `cli` surface
```

```python
import json, subprocess

proc = subprocess.run(
    ["./examples/hugr-docs/dist/hugr-docs", "./docs", "How do I resume a trace?", "--json"],
    capture_output=True, text=True,
)
answer = json.loads(proc.stdout)
print(answer["status"], answer["trace_id"], answer["metadata"]["cost_micro_usd"])
```

This approach returns the response as a plain dict instead of generated types, but provides full process isolation. Hugr uses the same mechanism when one agent grants another as a tool. The binary also has audit subcommands worth knowing: `--describe` (the agent card), `--config` (the parsed manifest + response schema), `--traces`, and `--stats`.

## Surface 3: `--mcp-serve`

The same binary can serve itself as an MCP server over stdio, so any MCP client (an editor, another agent framework, a Python MCP SDK) can call it as a tool without knowing anything about Hugr:

```bash
./examples/hugr-docs/dist/hugr-docs ./docs --mcp-serve
```

Register that command line in your MCP client's config and the agent shows up as a callable tool; each call is one ask, and traces still accumulate under `~/.hugr/hugr-docs/`.

## Choosing between them

- **Wheel:** you control the Python codebase and want typed responses, IDE completion, and in-process speed.
- **Subprocess:** you want isolation, a non-Python caller, or the smallest possible integration surface (spawn + parse JSON).
- **MCP:** the caller is an existing MCP-speaking host and you don't want any bespoke glue.

Whichever you pick, the agent's definition, privileges, traces, and cost accounting are identical; the surface is only packaging.

## Next

You've shipped a Rust-defined agent to Python. Next, define one in Python with tools as callables, config as data, and the same runtime: [An agent entirely in Python](05-agent-entirely-in-python.md).
